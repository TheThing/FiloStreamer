<Window x:Class="FiloStreamer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Vlc="clr-namespace:FiloStreamer.Controls"
        Closing="Window_Closing"
        Background="{StaticResource brushNormal}"
        Icon="logo.ico"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Vlc:VlcControl x:Name="vlcControl" Margin="0 0 0 8" />
        <!--<Border>
            <Vlc:VlcControl x:Name="vlcControl" Margin="0 0 0 8" />
        </Border>-->
        
        <Button Grid.Row="1" Click="Button_Click">Test</Button>
    </Grid>
</Window>
